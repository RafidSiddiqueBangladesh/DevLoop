# Environment variables for local development

# Message returned by /api/ping (can be customized)
PING_MESSAGE="pong"

# YouTube Data API key used by server route /api/youtube/search
# Obtain from Google Cloud Console and keep it secret (server-side usage)
YOUTUBE_API_KEY=""
